# Copy this file to .env and fill in your values
# Windows PowerShell alternative:
#   $env:GOOGLE_CLIENT_ID="your-client-id"
#   $env:GOOGLE_CLIENT_SECRET="your-client-secret"
#   $env:SECRET_KEY="a-secure-random-string"

# Flask
SECRET_KEY=change-me-in-production
DATABASE_URL=

# Google OAuth 2.0 (OIDC)
GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret-here
SERVER_NAME=localhost:5000
# If you run on 127.0.0.1:5000 but only whitelisted localhost, set this to localhost callback; or vice-versa
# GOOGLE_REDIRECT_URI=http://localhost:5000/auth/google/callback

# Development only: allow http for OAuth redirects
# OAUTHLIB_INSECURE_TRANSPORT=1

# Notes:
# - Authorized JavaScript origins: http://localhost:5000 and (optionally) http://127.0.0.1:5000
# - Authorized redirect URIs (add both):
#   http://localhost:5000/auth/google/callback
#   http://127.0.0.1:5000/auth/google/callback
